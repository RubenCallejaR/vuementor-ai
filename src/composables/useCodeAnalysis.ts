// src/composables/useCodeAnalysis.ts

export function useCodeAnalysis() {
  // TODO: estado reactivo (isLoading, error, result)
  // TODO: función analyzeCode(code: string) que llame a POST /api/analyze
}